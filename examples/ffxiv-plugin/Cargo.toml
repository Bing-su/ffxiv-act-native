[package]
name = "ffxiv-plugin"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
ffxiv-act-native = { workspace = true }

[build-dependencies]
ffxiv-act-native = { workspace = true, features = ["embedded-contracts"] }
