[package]
name = "ffxiv-plugin"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
act-bridge-gen = { path = "../.." }

[build-dependencies]
act-bridge-gen = { path = "../..", features = ["embedded-contracts"] }
